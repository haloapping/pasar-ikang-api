model Product {
  id          String   @id @default(ulid())
  slug        String   @unique
  name        String   @unique
  imageUrl    String
  price       BigInt
  unit        String
  description String
  stock       BigInt
  sold        BigInt
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
